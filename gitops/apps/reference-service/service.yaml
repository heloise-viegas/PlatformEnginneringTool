apiVersion: v1
kind: Service
metadata:
  name: reference-service
  namespace: reference-service
  labels:
    app: reference-service
spec:
  type: ClusterIP
  selector:
    app: reference-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
