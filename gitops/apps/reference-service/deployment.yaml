# Deprecated - replaced by the Helm chart at charts/reference-service.
# Argo CD's Application (gitops/argocd/bootstrap/reference-service.yaml) now
# sources from that chart instead of this raw manifest. Safe to delete once
# you've confirmed the chart-based deploy is working.
