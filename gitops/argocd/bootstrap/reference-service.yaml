# Deprecated - replaced by applicationset.yaml, which auto-generates an
# Application for every folder under charts/ (including this one). Delete the
# live "reference-service" Application object in the cluster before applying
# the ApplicationSet, otherwise the two will collide on the same name:
#   kubectl delete application reference-service -n argocd
