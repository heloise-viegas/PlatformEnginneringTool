name: payment-service CI/CD

on:
  push:
    branches: [main]
    paths:
      - "services/payment-service/**"
      - ".github/workflows/payment-service-ci.yml"
  workflow_dispatch: {}

jobs:
  build-and-deploy:
    uses: ./.github/workflows/service-ci-reusable.yml
    with:
      service_name: payment-service
      service_path: services/payment-service
      ecr_repository: payment-service
      values_path: charts/payment-service/values.yaml
    secrets:
      AWS_ACCESS_KEY_ID: \REDACTED_AWS_ACCESS_KEY_ID
      AWS_SECRET_ACCESS_KEY: \REDACTED_AWS_SECRET_ACCESS_KEY
